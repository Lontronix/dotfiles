This is a repository that Lonnie uses to share dotfiles across machines.

With the following exceptions, all newly created files should follow the following name scheme:

dot_{name}_{extension}.tmpl

where name and extension come from the type of file the template file will be used to generate.

For instance, 

Exceptions:
- Claude.md
- .chezmoi.toml.tmpl

